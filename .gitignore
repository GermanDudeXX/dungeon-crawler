__pycache__/
*.pyc
stats.json
save.json
settings.json

# PyInstaller build artifacts
build/
dist/
*.spec

# Written fresh before every build with that build's commit count, never
# committed - see constants.BUILD_VERSION_PATH / updater.py.
assets/build_version.txt
_update.bat

# Godot: import cache and the 1.3GB export templates are not source
godot/.godot/
godot/android/
